[pytest]
testpaths = tests
norecursedirs =
    .git
    .venv
    venv
    node_modules
    frontend/node_modules
    pytest-cache-files-*
    data/pytest-cache-files-*
addopts = -p no:cacheprovider
markers =
    live_llm: optional provider-backed smoke or evaluation tests that require real model credentials
